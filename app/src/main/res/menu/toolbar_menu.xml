<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Icono de notificaciones con badge numérico (vista personalizada) -->
    <item
        android:id="@+id/action_notificaciones"
        android:icon="@drawable/ic_notifications_white_24dp"
        android:title="Notificaciones"
        app:actionLayout="@layout/action_notificaciones"
        app:showAsAction="always" />

    <!-- Menú desplegable (3 puntos) -->
    <item
        android:id="@+id/action_configuracion"
        android:title="Configuración"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_perfil"
        android:title="Mi perfil"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_cerrar_sesion"
        android:title="Cerrar sesión"
        app:showAsAction="never" />

</menu>
