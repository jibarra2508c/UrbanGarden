<?xml version="1.0" encoding="utf-8"?>
<!--
    Vista personalizada para el item de notificaciones del toolbar.
    Contiene el icono de la campana y, encima, un globito rojo (badge)
    con el número de notificaciones pendientes.

    Se usa como app:actionLayout del item en toolbar_menu.xml.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/notif_action_view"
    android:layout_width="48dp"
    android:layout_height="48dp"
    android:background="?attr/selectableItemBackgroundBorderless"
    android:clickable="true"
    android:focusable="true"
    android:padding="8dp">

    <!-- Icono campana -->
    <ImageView
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:layout_gravity="center"
        android:src="@drawable/ic_notifications_white_24dp"
        android:contentDescription="Notificaciones" />

    <!-- Badge: globito rojo con el número -->
    <TextView
        android:id="@+id/tvBadgeNotif"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_gravity="top|end"
        android:background="@drawable/bg_badge_notif"
        android:gravity="center"
        android:includeFontPadding="false"
        android:text="0"
        android:textColor="#FFFFFF"
        android:textSize="10sp"
        android:textStyle="bold"
        android:visibility="gone" />

</FrameLayout>
